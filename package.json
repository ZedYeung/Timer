{
  "name": "timer-server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "body-parser": "^1.18.2",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "fs": "^0.0.1-security",
    "path": "^0.12.7",
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && yarn --production=false && yarn run build"
  }
}
