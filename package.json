{
  "name": "timer_server",
  "version": "0.1.0",
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.18.2",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "fs": "^0.0.1-security",
    "mongoose": "^5.0.16",
    "path": "^0.12.7"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && yarn --production=false && yarn run build"
  }
}
